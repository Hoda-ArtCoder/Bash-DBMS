#!/bin/bash
source ./Validations
source ./Common
source ./ConnectDatabase
function list_database() {
    if [ -d "$DB_DIR" ]
    then
        echo "-- -- -- Databases Lists available -- -- --"
        ls "$DB_DIR"
        navigate_menu
    else
        echo ">>>>> No Databases Found! <<<<<<"
        navigate_menu
    fi
}
